# TODO

- [ ] Jadikan Hero Section di Vite benar-benar background video bergerak
  - [ ] Update `vite-project/src/App.tsx`: tambahkan `<video>` di dalam header hero
  - [ ] Update `vite-project/src/App.css`: hapus styling `hero-bg::before` berbasis `background-image` mp4
  - [ ] Tambahkan overlay gradient agar teks tetap terbaca
  - [ ] Pastikan video tampil benar (muted, autoplay, loop, playsInline)
  - [ ] Jalankan `npm run dev` untuk testing visual
